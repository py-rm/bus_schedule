# Парсеры расписаний и новостей
requests>=2.31
beautifulsoup4>=4.12

# Запись JSON в Object Storage (Яндекс Облако / S3-совместимое хранилище,
# требуется только при развёртывании в облаке — см. DEPLOY.md)
boto3>=1.34

# Сборка настольного просмотрщика в .exe (только для разработки на Windows)
pyinstaller>=6.0
